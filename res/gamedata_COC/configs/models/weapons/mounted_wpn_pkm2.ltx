[mounted_weapon_definition]
fire_bone       = wpn_body              ; fire bone name
actor_bone      = wpn_body            ; actor bone name
rotate_x_bone   = wpn_body          ; rotate X-axis(pitch) bone name
rotate_y_bone   = wpn_body          ; rotate Y-axis(head)  bone name
camera_bone     = wpn_body            ; rotate Y-axis(head)  bone name 

wpn_section     = stationary_mgun

